import React from 'react';

const Button = (props) => (
   <button
      stretched={true}
      type="error"
      mode="filled"
      size="s">
      Merge!
   </button>
 );
  
 export { Button as default }; 